box: krlmlr/r-test
build:
  steps:
    - script:
        name: install
        code: apt-get update && apt-get install strace
    - script:
        name: install
        code: if ! strace -e trace=file -y Rscript -e 'install.packages("crayon")'; then fail "Install failed"; fi
